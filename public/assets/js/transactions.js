$(document).ready(function(){
    $('.datepicker').datepicker({
        format: 'yyyy-mm-dd', // Set the format you prefer
        autoclose: true,
        todayHighlight: true,
        // Add any other options you need
    });
});